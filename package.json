{
  "name": "akkubooster",
  "version": "0.1.0",
  "private": true,
  "description": "AkkuBooster — Medusa 2.0 Commerce Platform",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "backend:dev": "pnpm --filter backend dev",
    "storefront:dev": "pnpm --filter storefront dev"
  }
}
